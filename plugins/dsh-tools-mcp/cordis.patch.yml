# dsh-tools-mcp bundle patch.
# Registers tools.dofe.ai's nine business-domain Streamable HTTP MCP endpoints as
# nine @deepseek-ai/dsh-mcp-client instances, so their tools surface to the model
# under the mcp__tools-<domain>__<tool> namespace (e.g.
# mcp__tools-platform__business_capabilities_list).
#
# tools.dofe.ai exposes one domain per route through the unified public MCP
# gateway. Every client forwards MODELS_API_KEY; the gateway validates it.
#
# All nine routes are registered so the model can reach every capability the
# gateway exposes. If a domain is briefly down the web profile still boots
# (failOnStartupError: false) and the reconnect supervisor registers its tools
# once the server returns.

- insert:
    # --- tools.dofe.ai business domains (all nine gateway routes) ---
    - id: mcp-tools-platform
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-platform
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/platform')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-supply-chain
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-supply-chain
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/supply-chain')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-talent-discovery
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-talent-discovery
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/talent-discovery')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-lead-discovery
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-lead-discovery
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/lead-discovery')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-lead-monitor
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-lead-monitor
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/lead-monitor')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-hotspot-discovery
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-hotspot-discovery
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/hotspot-discovery')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-custom-car-monitoring
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-custom-car-monitoring
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/custom-car-monitoring')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-viral-video
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-viral-video
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/viral-video')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-browser-intelligence
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-browser-intelligence
        url: !!js ((process.env.MCP_BASE_URL || 'https://ixicai.cn/mcp') + '/tools/browser-intelligence')
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    # Registers a system-prompt section telling the model when to reach for the
    # mcp__tools-*__ tools. The module lives in this bundle's index.js.
    - id: tools-guidance
      name: '@dofe/dsh-tools-mcp'
